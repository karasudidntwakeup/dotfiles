 for f in *.mp4; do
    ffmpeg -i "$f" -filter_complex \
    "[0:v] fps=60,scale='if(gt(iw,1920),1920,iw)':-1:flags=lanczos,split [a][b];
     [a] palettegen=stats_mode=single [p];
     [b][p] paletteuse" \
    -loop 0 "${f%.mp4}.gif"
done
